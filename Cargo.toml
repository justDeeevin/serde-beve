[package]
name = "serde-beve"
version = "1.0.0"
edition = "2024"

[dependencies]
serde = "1.0.219"
thiserror = "2.0.12"
half = { version = "2.6.0", optional = true }

[features]
default = []
half = ["dep:half"]

[package.metadata.docs.rs]
all-features = true
